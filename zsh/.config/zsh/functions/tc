# Return a tree using colorls
# Args:
#   LEVELS : The number of levels, by default 3
# shellcheck disable=SC2168
local levels="${1:-3}"
if [ "$#" -gt 0 ]; then
    shift
fi
colorls -t --sort-dirs --tree="${levels}" "$@"
