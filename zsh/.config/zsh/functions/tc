# Output a tree using Color LS
# Assumes Color LS is installed
# Args:
#     DEPTH: The recursive depth of the tree. By default 2
#     PARAMS: Extra parameters to pass to colorls
local DEPTH="${1:-2}"
if [ "$#" -gt "0" ]; then
    shift 1
fi
local PARAMS="$@"
colorls --tree="${DEPTH}" "$@"
